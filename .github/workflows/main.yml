name: Run JMeter Tests

on:
  push:
    branches:
      - main

jobs:
  jmeter_test:
    runs-on: windows-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v2

      - name: Set Up Java
        uses: actions/setup-java@v2
        with:
          distribution: 'adopt'
          java-version: '11'

      - name: Run JMeter Tests
        run: |
          cd "C:\Users\ocina\OneDrive\Masaüstü\apache-jmeter-5.4.1\apache-jmeter-5.4.1\bin"
          .\jmeter.bat -n -t "C:\Users\ocina\OneDrive\Masaüstü\View Results Tree.jmx" -l results.jtl
